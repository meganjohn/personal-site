{% extends '../layouts/shell.njk' %}

{% block content %}

{% include "./banner-image.njk" %}

<main>
    <div class="main-body">
        <div class="page-title centered">
            <h1>{{ heading | safe }}</h1>
        </div>
        {{ content | safe }}
    </div>
</main>

{% endblock %}